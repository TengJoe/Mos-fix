# Mos 4.0.0 (20260220.1) - TengJoe Codex Fix

This release is based on upstream `Mos 4.0.0 (20260220.1)` and keeps the original feature set while adding a small set of targeted fixes for stability and compatibility.

## Highlights

- mitigates the `CVDisplayLink` scrolling crash path by copying the event before it crosses thread boundaries
- restores smooth and reverse scrolling in Dock folders and stack views by narrowing Launchpad detection
- improves reverse-scroll handling in iPhone Mirroring by recognizing `com.apple.ScreenContinuity`
- avoids double smoothing in UU Remote Desktop by treating `com.netease.uuremote` as a remote-control app

## Package identity

- fork: `TengJoe/Mos-fix`
- branch: `codex/fix-cvdisplaylink-crash`
- maintainer tag: `TengJoe Codex Fix`
- build display label inside the app: `4.0.0 · 20260220.1 · TengJoe Codex Fix`

## Notes

- This is a fork release, not an upstream official package.
- The app bundle name remains `Mos.app` for compatibility.
- Detailed implementation notes: [fork-fix-log-2026-03-05.md](./fork-fix-log-2026-03-05.md)

## 中文说明

这个版本基于上游 `Mos 4.0.0 (20260220.1)`，不增加新功能，重点是几项针对性的稳定性和兼容性修复。

主要变化：

- 通过在跨线程前复制滚动事件，降低 `CVDisplayLink` 相关闪退概率
- 修正 Dock 文件夹和叠放视图中的平滑/反转滚动
- 改善 iPhone 镜像中的反转滚动兼容性
- 避免 UU 远程桌面中的双重平滑

这是 fork 发布包，不是上游官方安装包。应用名仍保持为 `Mos.app`，但应用内版本文案会标注 `TengJoe Codex Fix`。
